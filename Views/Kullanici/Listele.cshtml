@model IEnumerable<IdentityUser>

<a class="btn btn-sm btn-primary" asp-controller="Kullanici" asp-action="Ekle">Yeni Kullanıcı Ekle</a>
<hr />
<table class="table table-sm table-bordered">
    <thead>
        <tr>
            <th width="25%">Id</th>
            <th>Kullanıcı Adı</th>
            <th>E-posta</th>
            <th>Telefon</th>
            <th>İşlem</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td>@user.PhoneNumber</td>
                <td>
                    <a asp-controller="Kullanici" asp-action="Duzenle" asp-route-id="@user.Id" class="btn btn-sm btn-primary">Düzenle</a>
                    <form action="Sil" method="post" onsubmit="return confirm('Emin misiniz?')" style="display: inline-block;">
                        <input type="hidden" name="id" value="@user.Id" />
                        <button type="submit" class="btn btn-sm btn-danger">Sil</button>
                    </form>
            </tr>
        }
    </tbody>
</table>